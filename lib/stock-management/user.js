import log from "#log";
import {Redis} from "#databases";

const
USER = {
  addShoppingCart() {
    // check if there is available stock
    // if yes -> reduce the available stock
    // if yes -> add to the stopping cart
    // if yes -> send back positive response

    // if no -> send back negative response
  },
  removeShoppingCart() {},
  deleteShoppingCart() {},
};
